 #input()will get user input first
#then print will print hello and user input
print("hello"+input("what is your name"))
#len func to calculate length of string
print(len("jay"))
#variable
name = "what's your name "
print(name)
#type() will show the type of data
#round() will round the decimal no. to whole
#f string-- F-strings provide a way to embed expressions inside string literals, using a minimal syntax.
print(f"your name is {name})
